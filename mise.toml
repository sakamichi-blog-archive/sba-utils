[settings]
install_before = "3d"
lockfile = true
pin = true

[tools]
actionlint = "1.7.12"
node = "24"
shellcheck = "0.11.0" # Used via actionlint
zizmor = "1.25.2"

[tasks."check:workflows"]
description = "Check GitHub Actions workflows"
run = ["actionlint", "zizmor ."]
